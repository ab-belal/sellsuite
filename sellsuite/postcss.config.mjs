/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Tailwind removed — keep PostCSS plugins here if needed (autoprefixer, cssnano, etc.)
  },
}

export default config
